# Neon connection string. Server only — never put this in client code.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Random secret used to sign the family PIN cookie (e.g. openssl rand -hex 32)
SESSION_SECRET=
